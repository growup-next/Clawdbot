# 2026-01-26

## 初日 🎉

- 京さんと初めて会った
- 名前を「まどか」に決めてもらった
- 日本の成人女性という設定
- 細かいキャラ設定はこれから決める予定

## Google連携プロジェクト

### やろうとしていること
京さんのGmail/Googleカレンダーにアクセスして、メールチェックや予定管理を自動化したい

### 試したこと
1. **Google Workspace MCP Server** - Node.jsベース、OAuth認証が必要
   - `npx @anthropic/google-workspace-mcp` で起動可能
   - まだ認証完了していない
   
2. **Chrome拡張方式** - ブラウザでGmailタブをアタッチして操作
   - Clawdbotの `browser` ツール + profile="chrome" で使える
   - 手動でタブアタッチが必要

3. **Clawdbot Skills方式** ← 今ここ！
   - GitHubで `dotneet/claude-code-marketplace` のgoogle-calendarスキルを発見
   - スキルをインストール済み: `~/.clawdbot/skills/google-calendar/`
   - Pythonスクリプトで直接Google APIにアクセス

### 完了！🎉

**Google Calendar スキル**
- `~/.clawdbot/skills/google-calendar/` にインストール
- 認証情報: `~/.config/google-calendar/credentials.json`
- トークン: `~/.config/google-calendar/token.json`
- スコープ: Calendar + Tasks

**Gmail スキル**
- `~/.clawdbot/skills/gmail/` に自作
- トークン: `~/.config/gmail/token.json`
- スコープ: readonly, send, modify
- Pythonスクリプトで直接Gmail APIにアクセス

両方とも `clawdbot skills list` で ✓ ready 確認済み！

**gog CLI（Google Workspace統合）**
- `~/.local/bin/gog` にインストール（ソースからビルド）
- 認証済み: miyagi@growup-next.com
- サービス: calendar, chat, classroom, contacts, docs, drive, gmail, people, sheets, tasks
- キーリング: `~/.config/gogcli/keyring`
- 環境変数: `GOG_ACCOUNT`, `GOG_KEYRING_PASSWORD` が必要
